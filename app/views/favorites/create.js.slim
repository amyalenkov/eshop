| $('#for_favorite_#{@product.id}').html("
= escape_javascript (render partial: 'cart_items/favorite_product')
| ");
| $('#tr_product_#{@product.id}').addClass("highlight_row_click");
| $('.cart_menu').html("
= escape_javascript (render partial: 'layouts/partials/cart_menu')
| ");

| $('##{@product.id}').html("
= escape_javascript (render partial: 'products/add_in_cart')
| ");
| $('##{@product.id}').show();

| $('##{@product.id}_show_add_in_cart').html("
= escape_javascript (render partial: 'products/add_in_cart_from_show')
| ");
| $('##{@product.id}_show_add_in_cart').show();

| $('##{@product.id}').html("
= escape_javascript (render partial: 'products/add_in_cart_other_product')
| ");
| $('##{@product.id}').show();